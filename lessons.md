lessons learned:
well the GitHub is set up after a week I started so maybe missing some things i learnt.

1.Functions:Blocks of code that is reusable in a program.
can be used for:
reusability
modularity(modular structure like nodes)
good for code reading.
important for learning python and leveling up as a programmer.

it is defined using the ‘def' key word in python.

you can call a function using its name and using () after.

can use a function inside another function(nested functions)

2.return vs print:
 return statement closes a function before returning value back to the caller.

in comparison print() is used for displaying the value in the screen.

3.python uses something called pass-by-object-reference,
from what i understand:
 inside a function, python creates a function variable that also points to the same value of the caller variable.and immutable objects like integers gets reassigned to these variables with a different object that it points to,while mutable ones like lists may get changed as both the function variable and caller variable shares the same list.idk of that made sense to you but for the time being I got somewhat of its idea.and I still  need to learn global variables and string,lists,and things like that my next step.
